import React from "react";

const Login = () => {
    const container = {

    };

    return (
        <div style={{
            justifyContent: "center",
            display: "flex"
        }}>
            <div>
                Login
            </div>
        </div>
    );
}

export default Login;