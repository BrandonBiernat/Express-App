export type User = {
    UserId: string,
    Email: string,
    FirstName: string,
    LastName: string,
    PhoneNumber: string,
    Username: string,
}

export type UserRequest = {
    Email: string,
    FirstName: string,
    LastName: string,
    PhoneNumber: string,
    Username: string,
}